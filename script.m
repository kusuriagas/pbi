Calendario

let
    Origen = List.Dates(
                        #date(2017,01,01), //Fecha de Inicio
                        365*3,             //Fecha para cada dia por los proximo 3 años
                        #duration(1,0,0,0) // Especificar la duracion del periodo 1 = dia, 0 =hora, 0= minutos, 0=segundos
    )
in
    Origen
    
    
